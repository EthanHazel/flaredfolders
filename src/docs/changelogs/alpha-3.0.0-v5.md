# 3.0.0 Alpha v5

## Fixes

- Export buttons now export the file with the folder name as the filename instead of just using "icon" or "folder-%SIZE%"
- Export buttons now have support for i18n
- Optimized and removed unused CSS for export button components
- Made preset loader use aspect-ratio to maintain a consistent aspect ratio
